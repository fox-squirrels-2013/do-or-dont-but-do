<%= link_to "don't!", dodont_path(@dodont.id, :command => :dont), :method => :put %>


<%= link_to "do!", dodont_path(@dodont.id, :command => :do), :method => :put %>



<%#= link_to "display list", dodonts_path %>
